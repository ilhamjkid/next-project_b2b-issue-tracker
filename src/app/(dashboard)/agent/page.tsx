import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
};

export default function AgentDashboardPage() {
  return <h1 className="text-4xl font-semibold">Agent Dashboard</h1>;
}
